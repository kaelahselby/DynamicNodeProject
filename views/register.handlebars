<p>
 {{#if warn}}
<div class="alert alert-danger"><strong>ERROR! </strong> Username taken! </div>
 {{/if}}
</p>
<p>
 {{#if warn2}}
<div class="alert alert-danger"><strong>ERROR! </strong> Username doesn't exist, Sign Up to login! </div>
 {{/if}}
</p>
<p>
 {{#if warn3}}
<div class="alert alert-danger"><strong>ERROR! </strong> Passwords do not match!!  </div>
 {{/if}}
</p>
<p>
 {{#if warn4}}
<div class="alert alert-danger"><strong>ERROR! </strong> {{warning}} must be longer than 6 characters!  </div>
 {{/if}}
</p>
<div class="panel panel-default">
  <div class="panel-heading">Sign up for Timer</div>
  <div class="panel-body">
  	<form name = "registerForm" id = "registerForm" method = "post" action = "/registerSubmit">
  <div class="form-group">
    <label for="userid">Username:</label>
    <input type="text" class="form-control" id="userid" name="userid" value = {{un}}>
  </div>
  <div class="form-group">
    <label for="pwd">Password:</label>
    <input type="password" class="form-control" name = "password" id="password" value = {{pw}}>
  </div>
<div class="form-group">
    <label for="pwd">Confirm Password:</label>
    <input type="password" class="form-control" name = "password2" id="password2" value = {{pw}}>
  </div>
  <div class="form-group">
    <label for="email">Email address:</label>
    <input name = "email" type="email" class="form-control" id="email" value = {{email}}>
  </div>
  <button type="submit" id = "loginButton" class="btn btn-default">Submit</button>
</form>
  </div>
</div>
